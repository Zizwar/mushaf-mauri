# مصحف موري V2 - تقرير التقدم

## 📋 نظرة عامة

ترقية مشروع مصحف موري من React Native قديم (Expo SDK 42) إلى Expo 54 مع هيكلة حديثة.

## ✅ ما تم إنجازه

### 1. إنشاء المشروع الجديد
- [x] إنشاء مشروع Expo 54 جديد باستخدام TypeScript
- [x] تثبيت المكتبات الأساسية:
  - `react-native-paper` - مكتبة UI مع دعم Dark Mode
  - `@react-navigation/*` - نظام التنقل
  - `zustand` - إدارة الحالة (بديل Redux أبسط)
  - `i18next` + `react-i18next` - تعدد اللغات
  - `expo-font`, `expo-av`, `expo-sqlite`

### 2. هيكل المجلدات الحديث
```
mushaf-mauri-v2/
├── src/
│   ├── components/     # المكونات المشتركة
│   ├── screens/        # الشاشات
│   ├── navigation/     # نظام التنقل
│   ├── theme/          # نظام الثيمات
│   ├── i18n/           # الترجمات (4 لغات)
│   ├── store/          # Zustand store
│   ├── services/       # Data Layer
│   ├── data/           # بيانات JSON
│   ├── constants/      # الثوابت
│   ├── types/          # TypeScript types
│   └── utils/          # دوال مساعدة
├── assets/
│   ├── fonts/          # خطوط PNU
│   ├── images/         # صور واجهة
│   └── pages/          # صفحات المصحف
└── App.tsx
```

### 3. نظام الثيمات (Theme)
- [x] دعم الوضع الليلي (Dark Mode)
- [x] 5 ثيمات ألوان (dark, light, sepia, blue, green)
- [x] تغيير الثيم بسهولة من أي مكان عبر `useTheme()`
- [x] حفظ الثيم المختار في AsyncStorage

### 4. نظام تعدد اللغات (i18n)
- [x] 4 لغات: العربية، الإنجليزية، الفرنسية، الأمازيغية
- [x] دعم RTL تلقائي
- [x] حفظ اللغة المختارة

### 5. طبقة البيانات (Data Layer)
- [x] واجهة `IDataSource` للتبديل بين JSON و SQLite
- [x] `DataService` singleton للوصول للبيانات
- [x] نقل بيانات الآيات والسور والإحداثيات

### 6. الشاشات المنجزة
- [x] `HomeScreen` - الشاشة الرئيسية
- [x] `WinoScreen` - عارض صفحات المصحف
- [x] `SurasScreen` - قائمة السور
- [x] `SearchScreen` - البحث في القرآن
- [x] `BookmarksScreen` - العلامات المرجعية
- [x] `SettingsScreen` - الإعدادات
- [x] `TafsirScreen` - عرض التفسير
- [x] `DrawerContent` - القائمة الجانبية

### 7. متغيرات التموضع (LAYOUT_CONFIG)
```typescript
// في src/constants/index.ts و WinoScreen.tsx
const LAYOUT_CONFIG = {
  MARGIN_PAGE: 55,
  MARGIN_PAGE_WIDTH: 5,
  NISBA: 1.471676300578035,
  SCALE_X: 1.0,  // تعديل حسب الشاشة
  SCALE_Y: 1.0,
  OFFSET_X: 0,
  OFFSET_Y: 0,
};
```

## 🔲 ما يحتاج إلى استكمال

### أولوية عالية
1. **نقل صفحات المصحف**
   - صور muhammadi (638 صفحة)
   - صور hafsTajweed (604 صفحة)
   - حالياً يتم تحميلها من الإنترنت

2. **تظليل الآيات (Verse Highlighting)**
   - ربط الإحداثيات بالصفحات
   - إضافة overlay للآية المحددة
   - تعديل SCALE_X/Y حسب الشاشة

3. **نظام الصوت (Audio)**
   - تشغيل التلاوة
   - اختيار القارئ
   - التكرار والإيقاف التلقائي

### أولوية متوسطة
4. **شاشة الختمة (Khitma)**
   - حساب الورد
   - تتبع التقدم

5. **شاشة المتجر (Store)**
   - تنزيل المحتوى

6. **PopUp الآية**
   - قائمة خيارات الآية (نسخ، مشاركة، علامة، تلاوة)

### أولوية منخفضة
7. **تحسينات**
   - الانتقال لـ SQLite
   - تحسين الأداء
   - اختبارات

## 📁 الملفات المهمة

| الملف | الوظيفة |
|-------|---------|
| `src/constants/index.ts` | الثوابت ومتغيرات التموضع |
| `src/theme/ThemeContext.tsx` | نظام الثيمات |
| `src/i18n/index.ts` | إعداد تعدد اللغات |
| `src/store/useAppStore.ts` | Zustand store |
| `src/services/DataService.ts` | طبقة البيانات |
| `src/screens/WinoScreen.tsx` | عارض المصحف الرئيسي |

## 🔧 كيفية التشغيل

```bash
cd mushaf-mauri-v2
npm start
# أو
npx expo start
```

## 📝 ملاحظات للمطور التالي

1. **الإحداثيات**: ملفات الإحداثيات في `src/data/coordinates/` تحتاج لضبط SCALE حسب حجم الشاشة

2. **الصور**: الصور محلياً في `assets/pages/` أو عبر الإنترنت من GitHub

3. **SQLite**: للانتقال لـ SQLite، فقط أنشئ class جديد يطبق `IDataSource`

4. **الثيمات**: لإضافة ثيم جديد، أضفه في `THEME_PRESETS` في `src/constants/index.ts`

---
آخر تحديث: 2024
